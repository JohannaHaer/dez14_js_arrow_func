

let hero = (heroName, heroPower, heroEnemy) => {
    let name = `${heroName}`
    let power = `${heroPower}`
    let enemy = `${heroEnemy}`
    console.log(`Mein Lieblingsheld ist: ${name}. Er hat die Fähigkeit: ${power} und sein größter Gegner ist: ${enemy}. Hihi`);
}

hero(`Victor`, `Süß zu sein`, `Daniel`)